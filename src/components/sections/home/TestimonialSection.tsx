import ContainerWidget from "@/components/widgets/ContainerWidget";

const TestimonialSection = () => {
  return (
    <section className="w-full bg-[#ECECEC] h-screen flex items-center justify-center z-40 relative">
      <ContainerWidget>
        <div className="text-black text-center">TestimonialSection</div>
      </ContainerWidget>
    </section>
  );
};

export default TestimonialSection;
