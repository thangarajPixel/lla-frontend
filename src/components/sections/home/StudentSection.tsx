const StudentSection = () => {
  return (
    <section className="w-full h-screen flex items-center justify-center bg-white z-40 relative">
      StudentSection
    </section>
  );
};

export default StudentSection;
